{% extends 'base.html.twig' %}

{% block title %}Detalle{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h2>{{task.title}}</h2>
    <p class="data-task">
        {{task.user.name}}  {{task.user.surname}} || {{task.createdAt|date('d/m/y h:i')}}
    </p>
    <p>{{task.content}}</p>
</div>
{% endblock %}
